<li>
	{{name}}

	<button class="change-devoured btn btn-primary" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if devoured}}Chow Down!{{else}}Choose another!{{/if}}
	</button>

	<button class="delete-burger btn btn-danger" data-id="{{this.id}}">DELETE!</button>
</li>
